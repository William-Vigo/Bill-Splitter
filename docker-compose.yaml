services:
  bill-splitter:
    image: golang:1.26
    working_dir: /app
    volumes:
      - .:/app
    command: go run main.go
    ports:
      - "8080:8080"
    networks:
      - bill-splitter
    restart: always
networks:
  bill-splitter:
    external: true
